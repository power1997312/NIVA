# =====================================================================
# 人工确认的跨域追溯边（需求链 ↔ 图纸链）
# =====================================================================
# 定位：这是两条链之间衔接的**正式**途径（docx §8.7 第 6 条：系统不做自动
#       语义挂接，由人工登记维护，避免跨域误关联）。
# 纪律：每条必须署名（reviewer）；登记即 CONFIRMED，进 provenance 留痕；
#       src 必须是图纸域节点（dia:...），dst 必须是文档域节点（doc:...）。
# 查节点 id：python -m niva.agent.orchestrator --manifest 或 utg_query
# =====================================================================
version: "1.0"
updated: "2026-09-10"
links: []
#  示例（去掉注释即可启用）：
#  - src: "dia:SAMA:loop:L01"
#    dst: "doc:DCS需求说明书:req:<DCS-SyRS001>"
#    relation: "implements"
#    note: "人工确认：L01 保护回路实现 SyRS001"
#    reviewer: "普京"
#    confidence: 1.0
